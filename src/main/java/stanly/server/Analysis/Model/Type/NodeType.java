package stanly.server.Analysis.Model.Type;

/**
 * @author Karuana
 *	노드의 타입을 정의해놓은 Enum이다. 
 */
public enum NodeType {
	PROJECT, LIBRARY, PACKAGE,PACKAGESET, CLASS,FIELD,METHOD, ENUM, INTERFACE, CONSTRUCTOR, ANNOTATION
}
